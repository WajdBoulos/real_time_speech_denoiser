# This file may be used to create an environment using:
# $ conda create --name <env> --file <this file> -c defaults -c conda-forge -c anaconda -c pytorch
# platform: win-64
# After this, you need to install torch_stft from pip:
torch_stft

# For AudioVisualizer
matplotlib==3.3.2
numpy==1.21.6

# For microphone and speakers
sounddevice==0.4.1

# For run script
pyyaml==5.4.1

# For DCCRN processor
librosa==0.8.1

torch==1.11.0
torchvision==0.12.0
torchaudio==0.11.0

#cudatoolkit # Use a version that works with the pytorch version and is installed on the computer
numba==0.55.1
ffmpeg

# For file reader and writer
pysoundfile

# for training
#tqdm
#visdom
#mir_eval
#pystoi
#pesq
#fairseq
